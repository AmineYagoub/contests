mutation UpdateQuestion($id: String!, $input: UpdateQuestionDto!) {
  updateQuestion(id: $id, input: $input) {
    id
    type
    title
    options
    authorId
    usedCount
    published
    created
    updated
  }
}
