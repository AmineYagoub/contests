mutation CreateQuestion($input: CreateQuestionDto!) {
  createQuestion(input: $input) {
    id
    type
    title
    level
    options
    authorId
    usedCount
    published
    created
    updated
  }
}
