query PaginateQuestions($params: QuestionPaginationDto!) {
  paginateQuestions(params: $params) {
    total
    data {
      id
      title
      type
      options
      lesson
      correctAnswer
      tags {
        title
      }
      created
      updated
      usedCount
    }
  }
}
